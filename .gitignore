DerivedData/
build/
*.xcuserstate
xcuserdata/
.DS_Store

# Local credentials. The Client ID is not an OAuth secret (PKCE),
# but it should not be versioned: each clone uses its own from the Dashboard.
.env
.env.*
Secrets.xcconfig
